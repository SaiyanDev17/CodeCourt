'use client'

export function useAuth() {
  // Auth logic will be implemented in Phase 4
  return {
    user: null,
    isAuthenticated: false,
    login: async () => {},
    logout: async () => {},
  }
}
